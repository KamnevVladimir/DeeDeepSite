import { motion } from 'framer-motion';

export default function FeaturesPage() {
  // features и остальной код полностью перенести из app/features/page.tsx
  return (
    <div className="min-h-screen bg-black text-white">
      {/* Hero Section, Features Grid, Technology Section, Comparison Section — полный код секций перенести из app/features/page.tsx */}
    </div>
  );
} 