// Удалить: import { motion } from 'framer-motion';

export default function DownloadPage() {
  // platforms, testimonials и остальной код полностью перенести из app/download/page.tsx
  return (
    <div className="min-h-screen bg-black text-white">
      {/* Hero Section, Platform Selection, Testimonials, Features Highlight — полный код секций перенести из app/download/page.tsx */}
    </div>
  );
} 